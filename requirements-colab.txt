# Colab already ships numpy, pandas, matplotlib, scikit-learn, torch.
# Kept for local fallback: pip install -r requirements-colab.txt
scikit-learn>=1.5
torch>=2.2
pandas>=2.2
matplotlib>=3.8
numpy>=1.26
pyarrow>=15.0
requests>=2.31
